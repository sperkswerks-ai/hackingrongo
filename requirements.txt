# =============================================================================
# hackingrongo — Rongorongo Hybrid Decipherment Pipeline
# Python 3.11 required.
# Pin minor versions; patch versions left open for security updates.
# =============================================================================

# ── Core ML framework ────────────────────────────────────────────────────────
torch>=2.2,<3.0
torchvision>=0.17,<1.0          # image transforms and pretrained model utilities

# ── Scientific computing ─────────────────────────────────────────────────────
numpy>=1.26
scipy>=1.12,<2.0

# ── Configuration management ─────────────────────────────────────────────────
hydra-core>=1.3,<2.0
omegaconf>=2.3,<3.0             # pulled in by hydra-core; explicit for clarity

# ── Experiment tracking ──────────────────────────────────────────────────────
mlflow>=2.12,<3.0

# ── Image processing ─────────────────────────────────────────────────────────
Pillow>=10.3,<11.0
opencv-python-headless>=4.9,<5.0   # cv2 used by zone_a/preprocessing.py
scikit-image>=0.22,<1.0             # skimage.morphology used by preprocessing.py

# ── Data analysis ────────────────────────────────────────────────────────────
pandas>=2.2,<3.0                    # compound_detector, compound_report, divergence_report
matplotlib>=3.8,<4.0                # analyze_embeddings.py visualisations
plotly>=5.20,<6.0                   # contact_analysis.py interactive bipartite plot
scikit-learn>=1.4,<2.0              # sklearn.cluster.HDBSCAN (analyze_embeddings.py)
umap-learn>=0.5,<1.0                # UMAP projection (analyze_embeddings.py)

# ── CLI ──────────────────────────────────────────────────────────────────────
click>=8.1,<9.0

# ── Serialisation ────────────────────────────────────────────────────────────
# pickle is stdlib; no extra package needed.
# ARPA-format LM loading uses the standard text parser in lm_scoring.py.

# ── Quantum annealing (Layer 4Q) ─────────────────────────────────────────────
dwave-samplers>=1.0                 # CPU simulated annealing QUBO solver (replaces neal)
dimod>=0.12                         # QUBO framework + TabuSampler
# dwave-ocean-sdk>=6.0 is optional for QPU access — NOT listed here (500 MB,
# requires a free Leap account).  Install separately: pip install dwave-ocean-sdk

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.1,<9.0
pytest-cov>=5.0,<6.0            # coverage reports for CI

# ── Development / notebook ──────────────────────────────────────────────────
jupyter>=1.0,<2.0
ipykernel>=6.29,<7.0

# ── Type checking (optional, not imported at runtime) ───────────────────────
# Run: mypy hackingrongo/
mypy>=1.9,<2.0
